---
"website": patch
---

Add llm.txt format spec and agent import section to homepage. Introduces a publicly served `/llm.txt` with the full import data schema so AI agents can convert existing tracker data into the JobNest import format. Adds a new homepage section explaining the privacy-first approach to data migration, lightweight header navigation, and a redesigned download section.
