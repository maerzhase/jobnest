---
"jobnest": patch
---

Keep local development data separate from the packaged app so testing in dev does not modify your main JobNest data.

Label the development app as `jobnest_dev` so it is easier to distinguish from the packaged app while testing.
