{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the default window",
  "windows": ["main"],
  "permissions": [
    "core:window:allow-close"
  ]
}
